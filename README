This plugin provides a protocol analyzer for the OSI presentation protocol (ISO
8823 / X.226) for use within Zeek. The analyzer enables Zeek to parse PPDUs and
automatically forwards pdus of the application layer to the corresponding
analyzer. For this purpose, the application layer analyzer must have the name
'util::canonify_name("ISO: "+oid)' (e.g. util::canonify_name("ISO:1.0.9506.4").
